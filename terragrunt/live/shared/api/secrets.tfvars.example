# Copy this file to secrets.tfvars (git-ignored — never commit the real value) and fill in
# the real key before running `terragrunt apply`. Left unset, RESEND_API_KEY is stored as an
# empty string and email sending won't work until this is set and the unit is re-applied.

resend_api_key = "re_xxxxxxxxxxxxxxxxxxxxxxxxxxxx"
